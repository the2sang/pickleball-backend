# ================================
# Production Profile
# ================================
spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres
    username: ${DB_USERNAME:pickleball.rkchvxoheiiabwplcfwy}
    password: ${DB_PASSWORD:alswl1!A314}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

# Logging - minimal for production
logging:
  level:
    com.pickleball: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
